create table ordered (
    OrderID INT PRIMARY KEY,
    OrderDate VARCHAR(50),
    CustomerID INT,
    TotalAmount DECIMAL(10, 2));
	insert into ordered(OrderID, OrderDate, CustomerID, TotalAmount)
	VALUES(001, 'Tuesday', 257, 500);

	VALUES 

   alter table ordered
   alter column OrderDate type VARCHAR(100);
   Select *from ordered
   

   